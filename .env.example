# Variables d'environnement pour Docker
# Copiez ce fichier en .env et adaptez les valeurs

# Configuration PostgreSQL
POSTGRES_PASSWORD=musicpassword_secure_2024

# Configuration SSL (optionnel)
SSL_CERT_PATH=./docker/ssl/cert.pem
SSL_KEY_PATH=./docker/ssl/key.pem

# Configuration domaine (optionnel)
DOMAIN=musiquelocale.com
EMAIL=admin@musiquelocale.com

PROMETHEUS_ENABLED=false
GRAFANA_ENABLED=false

# Configuration backup (optionnel)
BACKUP_ENABLED=false
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=7
